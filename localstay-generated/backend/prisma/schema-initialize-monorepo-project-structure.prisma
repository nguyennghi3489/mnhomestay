// LocalStay Initialize monorepo project structure - Prisma model

model InitializeMonorepoProjectStructure {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  // TODO: Add specific fields for Initialize monorepo project structure
  
  @@map("initialize_monorepo_project_structure")
}